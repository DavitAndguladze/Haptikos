import SwiftUI

/// Placeholder for `feat/ios-networking` — QR capture + URL handoff to `SocketManager`.
struct QRScannerView: View {
    var body: some View {
        Text("QR scanner (Samuel)")
            .padding()
    }
}

#Preview {
    QRScannerView()
}
